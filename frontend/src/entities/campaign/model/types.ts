import type { components } from '@/shared/api/schema.gen'

/** One campaign as this API answers for one — ours to edit, whoever built it. */
export type Campaign = components['schemas']['CampaignResponse']
